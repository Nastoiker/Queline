{
    "private": true,
    "type": "module",
    "scripts": {
        "front:serve": "cd Frontend && npm run dev",
        "front:build": "cd Frontend && npm run  build",
        "serve": "concurrently \"php artisan serve --port=8080\" \"npm run front:serve\"",
        "deploy": "npm run  setup && npm run front:build && php artisan migrate"
    },
    "dependencies": {
        "concurrently": "^8.0.1"
    }
}
